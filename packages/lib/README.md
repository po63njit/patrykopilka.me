# `@patrykopilka/lib` (scaffold)

Shared logic/types workspace package.

## Intended use

- Common TypeScript types and interfaces
- Shared utility helpers used across `apps/web` and `apps/api`
- Small pure functions with test coverage

## Current status

Scaffold only (no exported modules yet).
