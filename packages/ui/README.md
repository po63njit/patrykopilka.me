# `@patrykopilka/ui` (scaffold)

Shared UI workspace package.

## Intended use

- Reusable presentational components shared across apps
- Shared styling tokens and utility classes
- Optional Astro-compatible wrappers/components

## Current status

Scaffold only (no exported components yet).
